/*
//import { ContainerFormProps } from "utils";

const ContainerForm: React.FC<ContainerFormProps> = ({ bgColor }) => {
  return (
    <div className="border border-[--color-green] p-2 capitalize w-full h-10 rounded-md flex gap-2 items-center justify-between"></div>
  );
};

export default ContainerForm;
*/
